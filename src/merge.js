function json(value) {
  return JSON.stringify(value).replace(/\u2028/g, "\\u2028").replace(/\u2029/g, "\\u2029");
}

export function generateFailOpenScript(reason = "upstream unavailable") {
  const safeReason = String(reason).replace(/[\r\n]+/g, " ").slice(0, 240);
  return `// BiliMerge fail-open: ${safeReason.replace(/\*\//g, "* /")}\ntry {\n  if (typeof $response !== "undefined") { $done($response); }\n  else if (typeof $request !== "undefined") { $done($request); }\n  else { $done({}); }\n} catch (e) { $done({}); }\n`;
}

export function generateMergedScript({
  type,
  sourceA,
  sourceB,
  moduleA,
  moduleB,
  dispatch,
  timeoutMs = 10_000,
  generatedAt = new Date().toISOString(),
}) {
  if (type !== "request" && type !== "response") throw new Error(`Invalid merge type: ${type}`);
  if (typeof sourceA !== "string" || typeof sourceB !== "string") {
    throw new TypeError("Both upstream bundle sources are required");
  }
  // Scope shadowing can only capture lexical $done references. Refuse a changed
  // upstream that explicitly calls the real global, preserving one outer $done.
  // These global-property forms bypass the injected lexical $done and could
  // call Surge's real callback before the wrapper finishes.
  const globalDone = /\b(?:globalThis|window|self)\s*(?:(?:\?\.|\.)\s*\$done\b|(?:\?\.)?\s*\[\s*["']\$done["']\s*\])/;
  if (globalDone.test(sourceA) || globalDone.test(sourceB)) {
    throw new Error("Upstream bundle explicitly references a global $done");
  }
  const dispatchKey = type === "request" ? "http-request" : "http-response";
  const table = dispatch?.[dispatchKey];
  if (!table) throw new Error(`Missing dispatch table for ${dispatchKey}`);

  const keysA = moduleA.arguments.map(({ key }) => key);
  const keysB = moduleB.arguments.map(({ key }) => key);
  const summary = `A ${moduleA.version} + B ${moduleB.version}; ${type}; both=${table.both.length}`;

  return `// BiliMerge generated ${generatedAt}\n// ${summary}\n(() => {\n  "use strict";\n  const REAL_DONE = $done;\n  const IS_RESPONSE = ${type === "response"};\n  const SRC_A = ${json(sourceA)};\n  const SRC_B = ${json(sourceB)};\n  const KEYS_A = ${json(keysA)};\n  const KEYS_B = ${json(keysB)};\n  const DISPATCH = ${json({ a: table.aOnly, b: table.bOnly, both: table.both })};\n  const TIMEOUT_MS = ${Math.max(1, Number(timeoutMs) || 10_000)};\n  const ORIGINAL_ARGUMENT = typeof $argument === "undefined" ? "" : String($argument);\n  const ORIGINAL_INPUT = IS_RESPONSE\n    ? (typeof $response === "undefined" ? {} : $response)\n    : (typeof $request === "undefined" ? {} : $request);\n  const AsyncFunction = Object.getPrototypeOf(async function () {}).constructor;\n  let finished = false;\n\n  function finish(payload) {\n    if (finished) return;\n    finished = true;\n    REAL_DONE(payload);\n  }\n\n  function splitArgument(input) {\n    const parts = [];\n    let start = 0;\n    let quote = null;\n    let escaped = false;\n    for (let i = 0; i < input.length; i += 1) {\n      const char = input[i];\n      if (escaped) { escaped = false; continue; }\n      if (char === "\\\\") { escaped = true; continue; }\n      if (quote) { if (char === quote) quote = null; continue; }\n      if (char === "\\\"" || char === "'") { quote = char; continue; }\n      if (char === "&") { parts.push(input.slice(start, i)); start = i + 1; }\n    }\n    parts.push(input.slice(start));\n    return parts.filter(Boolean);\n  }\n\n  function buildArg(prefixed, keys, prefix) {\n    const wanted = new Set(keys);\n    const values = new Map();\n    for (const part of splitArgument(String(prefixed || "").replace(/^\\?/, ""))) {\n      const index = part.indexOf("=");\n      const rawKey = (index < 0 ? part : part.slice(0, index)).trim();\n      const expectedPrefix = prefix + ".";\n      if (!rawKey.startsWith(expectedPrefix)) continue;\n      const key = rawKey.slice(expectedPrefix.length);\n      if (wanted.has(key)) values.set(key, index < 0 ? "" : part.slice(index + 1));\n    }\n    return keys.filter((key) => values.has(key)).map((key) => key + "=" + values.get(key)).join("&");\n  }\n\n  function matches(pattern, url) {\n    try { return new RegExp(pattern).test(url); }\n    catch (error) { console.log("[BiliMerge] invalid dispatch pattern", pattern, error); return false; }\n  }\n\n  function triage(url) {\n    const aMatch = DISPATCH.a.some((pattern) => matches(pattern, url));\n    const bMatch = DISPATCH.b.some((pattern) => matches(pattern, url));\n    if (aMatch && bMatch) return "ab";\n    if (aMatch) return "a";\n    if (bMatch) return "b";\n    return "none";\n  }\n\n  function cloneNative(value) {\n    if (!value || typeof value !== "object") return value;\n    if (value instanceof ArrayBuffer) return value.slice(0);\n    if (ArrayBuffer.isView(value)) return new Uint8Array(value.buffer.slice(value.byteOffset, value.byteOffset + value.byteLength));\n    const copy = { ...value };\n    if (value.headers && typeof value.headers === "object") copy.headers = { ...value.headers };\n    if (value.body instanceof ArrayBuffer) copy.body = value.body.slice(0);\n    else if (ArrayBuffer.isView(value.body)) copy.body = new Uint8Array(value.body.buffer.slice(value.body.byteOffset, value.body.byteOffset + value.body.byteLength));\n    if (value.bodyBytes instanceof ArrayBuffer) copy.bodyBytes = value.bodyBytes.slice(0);\n    else if (ArrayBuffer.isView(value.bodyBytes)) copy.bodyBytes = new Uint8Array(value.bodyBytes.buffer.slice(value.bodyBytes.byteOffset, value.bodyBytes.byteOffset + value.bodyBytes.byteLength));\n    return copy;\n  }\n\n  async function runBlock(label, src, argument, input) {\n    globalThis.$argument = argument;\n    const isolatedInput = cloneNative(input.value);\n    if (input.kind === "response") globalThis.$response = isolatedInput;\n    else globalThis.$request = isolatedInput;\n    let captured;\n    let capturedFlag = false;\n    let active = true;\n    let resolveCaptured;\n    const capturedPromise = new Promise((resolve) => { resolveCaptured = resolve; });\n    const shadowDone = (payload) => {\n      if (active && !capturedFlag) {\n        capturedFlag = true;\n        captured = payload;\n        resolveCaptured();\n      }\n    };\n    let timer;\n    try {\n      const fn = new AsyncFunction("$done", src);\n      const invocation = Promise.resolve().then(() => fn(shadowDone));\n      const rejected = invocation.then(\n        () => new Promise(() => {}),\n        (error) => Promise.reject(error),\n      );\n      await Promise.race([\n        capturedPromise,\n        rejected,\n        new Promise((_, reject) => {\n          timer = setTimeout(() => reject(new Error(label + " timed out")), TIMEOUT_MS);\n        }),\n      ]);\n      active = false;\n      return { ok: capturedFlag, value: captured };\n    } catch (error) {\n      active = false;\n      console.log("[BiliMerge] " + label + " failed", error);\n      return { ok: false };\n    } finally {\n      if (timer !== undefined) clearTimeout(timer);\n    }\n  }\n\n  function has(object, key) {\n    return object != null && Object.prototype.hasOwnProperty.call(object, key);\n  }\n\n  function toNextResponse(payload, original) {\n    if (!payload || typeof payload !== "object") return original;\n    const next = { ...original };\n    if (has(payload, "status")) next.status = payload.status;\n    if (has(payload, "headers")) next.headers = payload.headers;\n    if (payload.bodyBytes != null) {\n      delete next.bodyBytes;\n      next.body = new Uint8Array(payload.bodyBytes);\n    } else if (has(payload, "body")) {\n      delete next.bodyBytes;\n      next.body = payload.body;\n    } else if (original && original.bodyBytes != null) {\n      delete next.bodyBytes;\n      next.body = new Uint8Array(original.bodyBytes);\n    }\n    return next;\n  }\n\n  function toNextRequest(payload, original) {\n    if (!payload || typeof payload !== "object" || has(payload, "response")) return original;\n    const next = { ...original };\n    for (const key of ["url", "headers", "body"]) {\n      if (has(payload, key)) next[key] = payload[key];\n    }\n    if (payload.bodyBytes != null) next.body = new Uint8Array(payload.bodyBytes);\n    return next;\n  }\n\n  (async () => {\n    const url = String((globalThis.$request && globalThis.$request.url) || ORIGINAL_INPUT.url || "");\n    const route = triage(url);\n    let current = { kind: IS_RESPONSE ? "response" : "request", value: ORIGINAL_INPUT };\n    let output = ORIGINAL_INPUT;\n    let changed = false;\n\n    if (route === "a" || route === "ab") {\n      const resultA = await runBlock("ADBlock", SRC_A, buildArg(ORIGINAL_ARGUMENT, KEYS_A, "ADBlock"), current);\n      if (resultA.ok) {\n        changed = true;\n        output = resultA.value;\n        if (route === "ab" && !IS_RESPONSE && resultA.value && typeof resultA.value === "object" && has(resultA.value, "response")) {\n          finish(resultA.value);\n          return;\n        }\n        if (route === "ab") {\n          current = {\n            kind: current.kind,\n            value: IS_RESPONSE\n              ? toNextResponse(resultA.value, current.value)\n              : toNextRequest(resultA.value, current.value),\n          };\n        } else current.value = resultA.value;\n      }\n    }\n\n    if (route === "b" || route === "ab") {\n      const resultB = await runBlock("Global", SRC_B, buildArg(ORIGINAL_ARGUMENT, KEYS_B, "Global"), current);\n      if (resultB.ok) { changed = true; output = resultB.value; }\n    }\n\n    finish(changed ? output : ORIGINAL_INPUT);\n  })().catch((error) => {\n    console.log("[BiliMerge] runtime failed open", error);\n    finish(ORIGINAL_INPUT);\n  });\n})();\n`;
}
